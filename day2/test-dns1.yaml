apiVersion: v1
kind: Pod
metadata:
  labels:
    run: test-dns1
  name: test-dns1
spec:
  containers:
  - image: nicolaka/netshoot
    name: test-dns
    command: ['sh', '-c', 'sleep 3600']
  dnsPolicy: ClusterFirst
  restartPolicy: Always
status: {}
